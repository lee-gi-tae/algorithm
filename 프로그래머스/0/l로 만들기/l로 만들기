def solution(myString):
    result = ""
    
    for i in range(len(myString)):
        if myString[i] < 'l':
            result += 'l'
        else:
            result += myString[i]
            
    return result
